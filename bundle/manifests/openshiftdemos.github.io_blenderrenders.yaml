apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: blenderrenders.openshiftdemos.github.io
spec:
  group: openshiftdemos.github.io
  names:
    kind: BlenderRender
    listKind: BlenderRenderList
    plural: blenderrenders
    singular: blenderrender
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BlenderRender is the Schema for the blenderrenders API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BlenderRenderSpec defines the desired state of BlenderRender
            properties:
              blend_file_location:
                description: BlendFileLocation is an HTTP(S) URL of a .blend file
                  with the embedded assets
                type: string
              output_filename:
                description: OutputFilename is the filename that will be created by
                  the blender run
                type: string
              render_type:
                description: RenderType specifies whether this is an `animation` or
                  a single `image` render
                type: string
            required:
            - blend_file_location
            - output_filename
            - render_type
            type: object
          status:
            description: BlenderRenderStatus defines the observed state of BlenderRender
            properties:
              render_job_status:
                type: string
            required:
            - render_job_status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
